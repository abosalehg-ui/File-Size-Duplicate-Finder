# 🔎 تقرير المراجعة الهندسية الشاملة — File Size Duplicate Finder v3.0

> **المراجِع:** مراجعة معمارية وأمنية وجودة شاملة (Principal-level review)
> **التاريخ:** 2026-07-19
> **النطاق:** كامل المستودع — `file_size_duplicate_finder.py` (2233 سطر)، `file_finder_cli.py` (279 سطر)، `build.spec`، ملفات التبعيات، الوثائق.
> **المنهجية:** قراءة كاملة للكود سطراً سطراً + فحص ثابت بـ `ruff 0.15` + اختبار وظيفي فعلي للـ CLI (وضع `full` مع ملفات مكررة — نجح بشكل صحيح).
> **حدود المراجعة:** لم يتم تشغيل الواجهة الرسومية (بيئة خادم بلا شاشة وبلا PyQt5)، لذا ملاحظات الواجهة مبنية على تحليل ثابت للكود؛ درجة الثقة فيها مذكورة عند كل بند.

---

## 1. الملخص العام لجودة المشروع

المشروع **أداة سطح مكتب ناضجة وظيفياً وبمستوى أعلى من المتوسط بوضوح** لمشروع فردي. المحرك الخوارزمي (فرز + نافذة منزلقة + تنقية hash متدرجة) **صحيح التصميم**، وعقلية الأمان (Dry-run، سلة المحذوفات، نسخ احتياطية دوّارة، استرجاع العمليات) **احترافية ونادرة في أدوات مماثلة**.

نقاط الضعف الرئيسية ليست في الخوارزميات بل في **الهندسة الإنشائية**:

1. **ملف واحد ضخم (Monolith)** يخلط المحرك والواجهة والتخزين.
2. **ازدواج المحرك بالكامل** بين GUI و CLI (نسختان مستقلتان من نفس الدوال — خطر انحراف حتمي).
3. **صفر اختبارات وصفر CI** رغم وجود `pytest` و `ruff` في `requirements-dev.txt`.
4. عدد من الأخطاء الفعلية متوسطة الخطورة في إدارة الخيوط والتحديد المفلتر (مفصّلة أدناه).

**التقييم الإجمالي:** جيد جداً وظيفياً (8/10)، متوسط إنشائياً (5/10)، ممتاز توثيقياً (9/10).

---

## 2. ✅ نقاط القوة (ما نُفّذ باحترافية)

| # | النقطة | لماذا هي احترافية |
|---|--------|-------------------|
| 1 | **الكشف المتدرج (Tiered detection)**: حجم ← partial hash ← SHA-256 كامل | نفس النهج المستخدم في أدوات ناضجة مثل `rdfind` و `jdupes`؛ يقلل قراءة الأقراص إلى الحد الأدنى |
| 2 | **تضمين حجم الملف داخل الـ partial hash** (`h.update(str(size).encode())`) — السطر 127 | يمنع تصادم ملفين لهما نفس البداية/النهاية وحجم مختلف — تفصيلة دقيقة يغفلها كثيرون |
| 3 | **`os.scandir` مع `follow_symlinks=False`** في كل مواضع المسح | أسرع من `os.walk`/`listdir+stat` ويمنع حلقات الروابط الرمزية اللانهائية |
| 4 | **Streaming hashing** (قراءة بمقاطع 1MB) | لا يحمّل الملفات الضخمة في الذاكرة أبداً |
| 5 | **نمط QThread + Signals صحيح** — الواجهة لا تُحدَّث إلا من خيط الواجهة عبر الإشارات | تجنّب أخطر أخطاء Qt الشائعة (لمس الـ widgets من خيط عامل) |
| 6 | **HashCache مع إبطال بـ (mtime, size)** | تصميم إبطال صحيح؛ لا يعيد استخدام hash لملف تغيّر |
| 7 | **منظومة أمان متكاملة**: Dry-run إجباري، send2trash، نسخ احتياطية دوّارة (آخر 10)، استرجاع كامل مع معالجة تصادم الأسماء (`_restored_N`) | عقلية "لا فقدان بيانات" واضحة في التصميم |
| 8 | **Throttling لإشارات التقدم** (كل 200 ملف) — السطر 327 | يمنع إغراق حلقة الأحداث بالإشارات |
| 9 | **استبعاد المجلدات النظامية** (`.git`, `node_modules`, `duplicates_sorted`...) بما فيها مجلد المخرجات نفسه | يمنع الأداة من "أكل ذيلها" عند إعادة المسح |
| 10 | **CSV بترميز `utf-8-sig`** | التفصيلة الوحيدة التي تجعل العربية تظهر صحيحة في Excel |
| 11 | **README ثنائي اللغة ممتاز** مع جداول ميزات ولقطات شاشة وخارطة طريق | توثيق أعلى من مستوى معظم المشاريع الفردية |
| 12 | **`.gitignore` نظيف** ويغطي ملفات بيانات التطبيق | صحيح |
| 13 | **إعداد High-DPI قبل إنشاء `QApplication`** — السطور 2210-2213 | الترتيب الصحيح الذي يفشل فيه كثيرون |

---

## 3. 🚨 قائمة الملاحظات المفصلة (مرتبة بالخطورة)

> لا توجد ملاحظات بمستوى **Critical** (لا فقدان بيانات مباشر ولا ثغرة قابلة للاستغلال عن بُعد — التطبيق محلي بالكامل). أعلى مستوى موجود هو **High**.

---

### H-1 — ازدواج محرك البحث بالكامل بين GUI و CLI

- **الوصف:** `file_finder_cli.py` يعيد تعريف `format_bytes`، `compute_partial_hash`، `compute_full_hash`، `scan_folder`، `group_sliding_window`، `refine_by_hash` كنسخ مستقلة عن نظيراتها في الملف الرئيسي. بل إن التعليق في السطر 22 يقول "نستخدم الدوال النقية من الملف الرئيسي" ثم لا يستوردها، ويترك `sys.path.insert` (سطر 23) كوداً ميتاً.
- **لماذا مشكلة:** أي إصلاح خطأ أو تحسين في نسخة لن ينتقل للأخرى (وقد حدث فعلاً: نسخة GUI من `compute_partial_hash` تعالج `OSError` وترجع `None`، ونسخة CLI ترمي الاستثناء ليُلتقط خارجياً؛ ونسخة CLI **لا تستخدم HashCache** إطلاقاً).
- **الخطورة:** 🔴 **High** (أخطر مصدر ديون تقنية في المشروع).
- **التأثير:** انحراف سلوكي حتمي بين الواجهتين مع الزمن؛ نتائج GUI و CLI قد تختلف لنفس المدخلات.
- **أفضل الممارسات:** Single Source of Truth — محرك واحد نقي (بلا Qt) تستهلكه الواجهتان.
- **الحلول الممكنة:**
  | الحل | المزايا | العيوب |
  |------|---------|--------|
  | (أ) استخراج حزمة `finder_core/` (scan, hash, group, cache, formats) واستيرادها من الطرفين | يحل الجذر؛ يمكّن الاختبارات؛ يتماشى مع "المرحلة 1" في خارطة الطريق | جهد يوم–يومين |
  | (ب) جعل CLI يستورد من ملف الـ GUI مباشرة | سريع | يفشل هدف "CLI بلا PyQt5" لأن استيراد الملف ينفّذ استيرادات Qt |
  | (ج) نقل الدوال النقية لملف `core.py` واحد فقط دون حزمة كاملة | 80% من الفائدة بـ 20% من الجهد | يترك الـ GUI ضخماً |
- **التوصية:** **(ج) فوراً ثم (أ) تدريجياً** — انقل الدوال النقية إلى `core.py` يستورده الطرفان، ثم وسّعه لاحقاً إلى حزمة. التبرير: يزيل الازدواج فوراً بأقل مخاطرة، ويفتح الباب لكتابة اختبارات على المحرك.

---

### H-2 — غياب كامل للاختبارات و CI

- **الوصف:** لا يوجد أي ملف اختبار في المستودع، ولا أي workflow لـ GitHub Actions، رغم إدراج `pytest` و `ruff` في `requirements-dev.txt`.
- **لماذا مشكلة:** هذه أداة **تنقل وتحذف ملفات المستخدمين**. الدوال الحساسة (التجميع بالنافذة المنزلقة، التقسيم بالامتداد، إبطال الـ cache، منطق إعادة التسمية عند التصادم، الاسترجاع) كلها قابلة للاختبار المعزول ولا يغطيها شيء. `ruff` نفسه يكتشف حالياً 20 ملاحظة لم يرها أحد لأنه لا يعمل آلياً.
- **الخطورة:** 🔴 **High**.
- **التأثير:** أي refactor (بما فيه H-1) سيتم "على المكشوف"؛ الانحدارات ستكتشف من المستخدمين.
- **أفضل الممارسات:** اختبارات وحدات للمحرك النقي + lint في CI على كل push.
- **الحلول الممكنة:**
  | الحل | المزايا | العيوب |
  |------|---------|--------|
  | (أ) pytest للمحرك النقي فقط (بلا Qt) + GitHub Actions (lint + test) | رخيص جداً؛ يغطي الجزء الخطر | لا يغطي الواجهة |
  | (ب) إضافة `pytest-qt` لاختبار الواجهة أيضاً | تغطية أشمل | جهد أكبر بكثير؛ هش في CI بلا شاشة (يحتاج xvfb) |
- **التوصية:** **(أ)** الآن — بعد تنفيذ H-1 مباشرة تصبح كتابة ~15 اختباراً للمحرك مسألة ساعات. `pytest-qt` لاحقاً فقط إن كبُر المشروع.

---

### H-3 — `closeEvent` لا يوقف خيط سلة المحذوفات (`trash_thread`)

- **الوصف:** في السطر 2178: `for thread in [self.search_thread, self.move_thread, self.restore_thread]` — القائمة **تنسى `self.trash_thread`**.
- **لماذا مشكلة:** إذا أغلق المستخدم النافذة أثناء عملية حذف جارية: (1) الخيط يستمر بحذف الملفات بعد "إغلاق" التطبيق، (2) عند تدمير كائن `QThread` وهو يعمل يطبع Qt التحذير `QThread: Destroyed while thread is still running` وقد ينهار التطبيق.
- **الخطورة:** 🔴 **High** (ثقة عالية — الخطأ ظاهر نصياً؛ لم يُشغَّل GUI للتحقق التجريبي).
- **التأثير:** سلوك غير متوقع عند الإغلاق + انهيار محتمل + استمرار حذف ملفات بعد أن ظن المستخدم أنه ألغى.
- **أفضل الممارسات:** كل الخيوط الحية تُدار من قائمة واحدة، لا قوائم يدوية تُنسى عند إضافة خيط جديد.
- **الحلول:** (أ) إضافة `self.trash_thread` للقائمة — سطر واحد. (ب) الاحتفاظ بـ `self._threads: list` تُسجَّل فيها كل الخيوط عند إنشائها ويمر عليها `closeEvent`.
- **التوصية:** **(أ) فوراً** (إصلاح السطر)، و**(ب)** ضمن إعادة الهيكلة لمنع تكرار نفس النسيان مستقبلاً.

---

### H-4 — التحديد يشمل الصفوف المخفية بالفلتر

- **الوصف:** `get_selected_files()` (سطر 1816) يجمع كل صف نصّ عموده الأول "☑" **دون فحص `isHidden()`**، و`select_all()` (سطر 1798) يحدد الصفوف المخفية أيضاً.
- **لماذا مشكلة:** سيناريو واقعي: المستخدم يبحث، يكتب في الفلتر `jpg`، يضغط "تحديد الكل" ظاناً أنه يحدد الصور الظاهرة فقط، ثم "🗑️ سلة المحذوفات" — **فتُحذف أيضاً كل الملفات المخفية غير الظاهرة**. نافذة الـ Dry-run تعرضها فعلاً (وهذا يخفف الخطر)، لكن قائمة بمئات الأسطر نادراً ما تُراجع سطراً سطراً.
- **الخطورة:** 🔴 **High** (خطر فقدان بيانات بمساعدة المستخدم؛ الثقة عالية من قراءة الكود).
- **التأثير:** حذف/نقل ملفات لم يقصدها المستخدم.
- **أفضل الممارسات:** العمليات الجماعية تطبَّق على ما يراه المستخدم (WYSIWYG selection).
- **الحلول:** (أ) تخطي `isHidden()` في `select_all` و `get_selected_files`. (ب) إبقاء السلوك الحالي مع تحذير صريح في الـ Dry-run: "منها N ملف مخفي بالفلتر الحالي".
- **التوصية:** **(أ) + سطر تنبيه من (ب)**: التحديد الجماعي يتجاهل المخفي، وإن وُجدت عناصر محددة مخفية (حُددت قبل الفلترة) يذكرها الـ Dry-run صراحة.

---

### M-1 — إيقاف البحث/الإغلاق يجمّد الواجهة (`thread.wait()` في خيط الواجهة)

- **الوصف:** `stop_search()` (سطر 1618) و `closeEvent` يستدعيان `wait()` بلا مهلة. علم الإيقاف `is_running` يُفحص **بين** الملفات فقط؛ `compute_full_hash` لا يفحصه داخل حلقة القراءة، فإيقاف أثناء تجزئة ملف 50GB يجمّد الواجهة حتى انتهاء تجزئة الملف الحالي بالكامل.
- **الخطورة:** 🟠 **Medium**.
- **التأثير:** "التطبيق لا يستجيب" لدقائق في أسوأ الحالات؛ انطباع بالتعليق.
- **أفضل الممارسات:** الإلغاء التعاوني يجب أن يصل إلى أعمق حلقة عمل؛ الانتظار يكون بمهلة أو غير حاجب.
- **الحلول:** (أ) تمرير callback/علم إلى دوال التجزئة يُفحص كل chunk. (ب) `wait(2000)` مع مؤشر "جاري الإيقاف..." وتعطيل الأزرار. (ج) `terminate()` — **مرفوض** (قتل خيط يكتب cache = فساد بيانات).
- **التوصية:** **(أ) + (ب) معاً**: فحص العلم كل chunk (يجعل الإيقاف شبه فوري) مع مهلة انتظار قصيرة كشبكة أمان.

---

### M-2 — تضارب حد التقارب: الواجهة لا تستطيع "التطابق الدقيق" الموعود

- **الوصف:** README يقول "0 = تطابق دقيق" و CLI افتراضيه `0.0`، لكن `threshold_spin.setRange(0.1, 1000)` (سطر 1294) يمنع الصفر في الواجهة. النتيجة: وضع "Partial/Full hash" في الواجهة يبدأ من نافذة 0.1MB (لا يضر لأن الـ hash يصفي لاحقاً، لكنه يوسّع العمل)، ووضع "حجم متقارب" **لا يمكنه أبداً** مطابقة الحجم الدقيق — وهو الوضع نفسه الذي يتيح زر الحذف إلى السلة.
- **لماذا مشكلة:** في وضع "حجم متقارب" بعتبة ≥0.1MB، "المكررات" ليست مكررات إطلاقاً — مجرد ملفات متقاربة الحجم — وزر سلة المحذوفات متاح عليها مباشرة.
- **الخطورة:** 🟠 **Medium** (فجوة توقعات + مخاطرة حذف؛ يخففها الـ Dry-run وقابلية الاسترداد من السلة).
- **الحلول:** (أ) `setRange(0.0, 1000)` مع نص توضيحي "0 = تطابق دقيق". (ب) توحيد الافتراضي مع CLI. (ج) إضافة تحذير في Dry-run عندما يكون الوضع "حجم فقط": "التطابق بالحجم لا يعني تطابق المحتوى".
- **التوصية:** **الثلاثة معاً** — كلها أسطر قليلة، وتُغلق فجوة توثيق/سلوك حقيقية.

---

### M-3 — `HashCache`: نمو غير محدود وكتابة غير ذرّية

- **الوصف:** الكاش في `~/.file_finder_hash_cache.json` لا يحذف مدخلات الملفات التي لم تعد موجودة (يتراكم للأبد)، ويُكتب بـ `open(...,'w')` مباشرة — انقطاع أثناء الكتابة = ملف JSON تالف (يُتعامل معه بالتجاهل، فيُفقد الكاش كله)، ويُحمَّل ويُحفَظ بالكامل في كل بحث.
- **الخطورة:** 🟠 **Medium** (أداء/موثوقية، ليس فقدان بيانات مستخدم).
- **التأثير:** بعد شهور من الاستخدام على مجلدات كبيرة: ملف بعشرات الميجابايت يُقرأ ويُكتب كل بحث، واحتمال فقدانه كلياً عند أي انقطاع.
- **الحلول:** (أ) كتابة ذرّية: `tmp + os.replace()` + تقليم المدخلات غير الموجودة عند الحفظ. (ب) الانتقال إلى SQLite (`sqlite3` قياسي). 
- **التوصية:** **(أ)** الآن (أسطر معدودة)؛ **(ب)** فقط إذا أصبح الكاش عنق زجاجة فعلياً (>~100K مدخل).

---

### M-4 — علّامات صغيرة في تدفق العمليات (مجموعة أخطاء منطقية متحقق منها نصياً)

كل بند هنا صغير لكنها مجتمعة تستحق Medium:

1. **`on_move_error` لا يعيد تفعيل `trash_btn`** (سطر 1969-1975) — بعد فشل نقل يبقى زر السلة معطلاً حتى بحث جديد.
2. **`restored: True` تُكتب حتى مع فشل جزئي** (`on_restore_finished` سطر 2000) — عملية أُرجع منها 5 من 100 ملف تُوسم "تم الإرجاع" ويُقفل زر الإرجاع عنها نهائياً. الأفضل: وسم "إرجاع جزئي" وإبقاء الزر متاحاً للمتبقي.
3. **`dropEvent` لا يحفظ `last_folder`** بينما `browse_folder` يحفظه — سلوك غير متسق بين مسارَي الإدخال.
4. **إعادة البحث تلقائياً بعد كل نقل/حذف/إرجاع** — في وضع SHA-256 على مجلد ضخم هذا يعني دقائق من العمل غير المطلوب دون سؤال المستخدم.
5. **`file_id = f"file_{group_idx}_{name}"`** — تصادم مؤكد لملفين بنفس الاسم في مجلدات مختلفة داخل نفس المجموعة (وارد جداً في البحث المتداخل). عملياً غير مؤذٍ حالياً لأن `file_paths` **لا يُقرأ أبداً** (انظر L-1) لكن أي استخدام مستقبلي له سيرث الخطأ.
- **الخطورة:** 🟠 **Medium** مجتمعة.
- **التوصية:** إصلاحها دفعة واحدة في PR صيانة صغير؛ كلها سطور مفردة عدا (2) التي تحتاج حقل `restored_count` في بنية السجل.

---

### M-5 — ألوان المجموعات الفاتحة تُعرض كما هي في الوضع الداكن

- **الوصف:** `display_results` يلوّن خلفيات المجموعات بـ `GROUP_COLORS` (باستيلات فاتحة مثل `#E3F2FD`) بينما نمط الوضع الداكن يجعل لون النص `#cdd6f4` (رمادي فاتح). نص فاتح على خلفية فاتحة = تباين شبه معدوم لصفوف المجموعات في الوضع الداكن.
- **الخطورة:** 🟠 **Medium** (ثقة متوسطة-عالية: استنتاج من الكود؛ يحتاج تأكيداً بصرياً بتشغيل الواجهة).
- **الحلول:** (أ) طقم ألوان ثانٍ داكن `GROUP_COLORS_DARK` يُختار حسب الوضع. (ب) ضبط لون نص العنصر صراحة مع الخلفية (`setForeground`).
- **التوصية:** **(أ)+(ب)**: اختيار الطقم حسب الوضع مع تثبيت لون النص صراحة لكل عنصر ملوَّن، وإعادة التلوين عند تبديل الوضع.

---

### M-6 — لا يوجد ملف LICENSE

- **الوصف:** README يعلن "All Rights Reserved" لكن لا يوجد ملف `LICENSE` في الجذر، والمستودع عام على GitHub.
- **لماذا مشكلة:** الغياب يجعل الوضع القانوني غامضاً للمستخدمين (افتراضياً: كل الحقوق محفوظة، أي لا يحق لأحد قانونياً حتى تشغيل نسخة معدلة)، وهذا يتناقض مع نشر تعليمات `git clone` والتثبيت في README.
- **الخطورة:** 🟠 **Medium** (حوكمة، لا تقنية).
- **الحلول:** (أ) إن كان القصد مشروعاً مفتوح المصدر: إضافة MIT/Apache-2.0. (ب) إن كان القصد حفظ الحقوق فعلاً: ملف LICENSE صريح بشروط الاستخدام الشخصي.
- **التوصية:** حسم النية وإضافة الملف — **قرار يعود للمالك**؛ التناقض الحالي (تعليمات استخدام عامة + كل الحقوق محفوظة) هو المشكلة، لا الاختيار نفسه.

---

### L-1 — كود ميت واستيرادات غير مستخدمة (20 ملاحظة ruff مؤكدة)

- **الوصف والدليل:**
  - `self.file_paths` يُنشأ ويُملأ (السطور 917، 1593، 1691) **ولا يُقرأ في أي مكان** — كود ميت.
  - `MoveOperation` و `OperationBatch` (dataclasses، السطور 205-227) معرّفتان **ولا تُستخدمان أبداً** (السجل يُبنى dict يدوياً).
  - `CONFIG_FILE` (سطر 70) غير مستخدم (استُبدل بـ QSettings).
  - `sys.path.insert` في CLI (سطر 23) بلا أثر.
  - 18 استيراداً غير مستخدم يكشفها `ruff --select F` (منها `QSound`/`QSoundEffect` اللذان يتحكمان بـ `SOUND_AVAILABLE` بينما `play_notification` يستدعي `QApplication.beep()` الذي **لا يحتاج QtMultimedia أصلاً** — أي أن الشرط نفسه بلا معنى، و`hiddenimports=['PyQt5.QtMultimedia']` في build.spec يضخّم الحزمة بلا داعٍ).
  - `except:` عارٍ في موضعين (سطر 624، 2092) و `except Exception as e` بمتغير غير مستخدم (سطر 2119).
- **الخطورة:** 🟢 **Low** (لا تأثير تشغيلي) لكنها **ضجيج يضلل القارئ** ويوحي بميزات غير موجودة.
- **التوصية:** `ruff check --fix` + حذف يدوي للـ dataclasses و `file_paths` و `CONFIG_FILE` وشرط الصوت، وإزالة QtMultimedia من build.spec. ثم تفعيل ruff في CI (H-2) لمنع العودة.

---

### L-2 — تكرار `format_size` أربع مرات

- **الوصف:** `format_bytes` (module-level)، `HistoryDialog.format_size`، `FileSizeDuplicateFinder.format_size`، ونسخة CLI — أربع نسخ لنفس الدالة (بسلوك متطابق عملياً).
- **الخطورة:** 🟢 **Low**.
- **التوصية:** نسخة واحدة في `core.py` (تُحل تلقائياً مع H-1).

---

### L-3 — ملاحظات CLI صغيرة

1. `--quiet` لا يكتم رسالة "✅ تم حفظ التقرير" (تحققت تجريبياً).
2. كود الخروج `1` عند "لا توجد مجموعات" منطق بأسلوب `grep` لكنه **غير موثق** في README — سكربت يعتبر ≠0 فشلاً سيلتبس.
3. لا خيار `--exclude` لإضافة مجلدات استبعاد، ولا `--threads`.
- **الخطورة:** 🟢 **Low**.
- **التوصية:** إخضاع الرسالة لـ `--quiet`، وتوثيق أكواد الخروج (0=وجد، 1=لم يجد، 2=خطأ استخدام) في `--help` و README.

---

### L-4 — ملاحظات دقة وتوافق منصات

1. **`st_ctime` يُعرض كـ"تاريخ الإنشاء"** (سطر 319): صحيح على Windows، لكنه على Linux "وقت تغيير الـ inode" وليس الإنشاء — معلومة مضللة للمستخدم على لينكس. البديل: `st_birthtime` حيث يتوفر مع سقوط مرتّب.
2. **خط `Segoe UI` مفروض على كل المنصات** (سطر 2219) — غير موجود على Linux/macOS (سيسقط Qt لبديل، لكن الأنظف اختيار حسب المنصة أو ترك الافتراضي).
3. **`upx=True` في build.spec** معروف أنه يكسر بعض Qt DLLs على Windows أحياناً — إن ظهرت أعطال غامضة في النسخة المبنية فهذا أول مشتبه به.
4. الوصولية: لا اختصارات لوحة مفاتيح للعمليات الرئيسية، والاعتماد على رموز ☐/☑ نصية بدل `Qt.ItemIsUserCheckable` يحرم قارئات الشاشة من فهم حالة التحديد.
- **الخطورة:** 🟢 **Low** إجمالاً.
- **التوصية:** استخدام checkboxes حقيقية (`setCheckState`) — يحسّن الوصولية **ويبسّط** منطق التحديد اليدوي الحالي كله دفعة واحدة.

---

### ملاحظة أمنية معلوماتية (ليست ثغرة)

- **MD5 في الـ partial hash**: استخدام مقبول هنا (فهرسة سريعة لا أمان)، والمطابقة النهائية في وضع "full" تتم بـ SHA-256 — تصميم سليم. فقط انتبه أن وضع **partial وحده يمكن نظرياً أن يجمع ملفين مختلفين** (نفس البداية والنهاية والحجم) — وهو موثق ضمنياً كـ"متوازن"، ويستحق جملة تحذير في tooltip زر الحذف عند وضع partial.
- **الخصوصية**: ملفات `~/file_finder_history.json` والكاش تخزن مسارات كاملة بنص صريح في مجلد المنزل — طبيعي لأداة سطح مكتب، يُذكر للاكتمال فقط.
- **لا مدخلات شبكية ولا تنفيذ أوامر ولا eval** — سطح الهجوم شبه معدوم. ✅

---

## 4. أبرز المخاطر التقنية (ترتيب تنازلي)

1. **Refactor بلا شبكة أمان**: أي محاولة لتنفيذ "المرحلة 1" (إعادة الهيكلة المعلنة في خارطة الطريق) قبل وجود اختبارات = مخاطرة انحدار في أداة تحذف ملفات.
2. **انحراف GUI/CLI** المستمر بسبب الازدواج (H-1).
3. **حذف غير مقصود** عبر تركيبة الفلتر+تحديد الكل (H-4) أو وضع "حجم فقط" (M-2).
4. **هشاشة الإغلاق** أثناء عمليات جارية (H-3).
5. **PyQt5 في أواخر عمره**: Qt5 خرج من الدعم المجتمعي؛ الترقية إلى **PyQt6/PySide6** ستصبح إلزامية خلال سنوات (Python الأحدث + منصات ARM). ليست عاجلة، لكن كل سطر GUI جديد يُكتب على Qt5 يزيد فاتورة الترحيل.

---

## 5. قائمة الأخطاء حسب الأولوية (خلاصة تنفيذية)

| أولوية | البند | الموضع | حجم الإصلاح |
|--------|-------|--------|-------------|
| 1 | إضافة `trash_thread` إلى closeEvent (H-3) | سطر 2178 | سطر واحد |
| 2 | تخطي الصفوف المخفية في التحديد (H-4) | 1798-1835 | ~10 أسطر |
| 3 | السماح بـ 0 في threshold + تحذير وضع الحجم (M-2) | 1294 | ~5 أسطر |
| 4 | إعادة تفعيل trash_btn في on_move_error (M-4.1) | 1969 | سطر واحد |
| 5 | فحص علم الإيقاف داخل حلقات التجزئة (M-1) | 114-143 | ~15 سطراً |
| 6 | كتابة ذرّية + تقليم للكاش (M-3) | 145-198 | ~15 سطراً |
| 7 | ألوان المجموعات في الوضع الداكن (M-5) | 74-78، 1664 | ~20 سطراً |
| 8 | restored الجزئي (M-4.2) | 1997-2004 | ~15 سطراً |
| 9 | تنظيف الكود الميت (L-1) | متفرق | `ruff --fix` + حذف |

---

## 6. فرص التحسين

### الأداء
- **التجزئة متسلسلة بالكامل**: `concurrent.futures.ThreadPoolExecutor` (الـ hashing يحرر GIL أثناء I/O وفي `hashlib`) يمنح تسريعاً 2-4× على SSD/NVMe للمجموعات الكبيرة. أكبر مكسب أداء متاح.
- **`display_results` مع آلاف المجموعات**: `setExpanded(True)` للجميع + عناصر فردية سيجعل الشجرة بطيئة عند 10K+ ملف (الرقم المعلن في README). الحلول: `setUpdatesEnabled(False)` أثناء الملء، طي المجموعات افتراضياً فوق حد معين، أو ترحيل صفحات النتائج.
- إعادة البحث التلقائية بعد كل عملية (M-4.4): اجعلها تحديثاً محلياً للشجرة (إزالة الصفوف المنقولة) بدل مسح كامل جديد.

### الأمان (بمعنى سلامة بيانات المستخدم)
- **حارس "الاحتفاظ بنسخة"**: خيار (مفعّل افتراضياً) يمنع تحديد كل ملفات المجموعة للحذف — الميزة الأعلى قيمة أماناً.
- كتابة سجل العمليات **قبل** بدء النقل (سجل نوايا) لا بعده، كي لا تضيع عملية انقطعت في منتصفها من السجل.
- تحذير في Dry-run عندما يكون مصدر التحديد وضع "حجم فقط" أو "partial" (ليست مطابقة مضمونة).

### المعمارية
- الهيكل المستهدف (يتوافق مع "المرحلة 1" المعلنة):
  ```
  finder/
  ├── core/          # scan.py, hashing.py, grouping.py, cache.py (نقي، صفر Qt)
  ├── ops/           # move.py, trash.py, restore.py, history.py
  ├── gui/           # main_window.py, dialogs.py, workers.py (QThread wrappers), styles.py
  ├── cli.py
  └── __main__.py
  ```
- فصل الـ QSS إلى ملفات/ثوابت مستقلة (حالياً ~350 سطر أنماط داخل منطق الواجهة).
- استخدام الـ dataclasses الموجودة أصلاً (`MoveOperation`, `OperationBatch`) بدل dicts الحرة — إما استخدامها فعلاً أو حذفها؛ وجودها غير المستخدم أسوأ الخيارين.

### تبسيط الكود وتقليل التعقيد
- **checkboxes حقيقية** بدل نصوص ☐/☑ تختصر `on_item_clicked`/`select_all`/`deselect_all`/`get_selected_files` إلى نصف حجمها وتصلح الوصولية معاً.
- توحيد خيوط العمليات الأربعة (بحث/نقل/سلة/استرجاع) في صنف `WorkerThread` واحد يستقبل دالة — الأربعة متطابقة البنية (progress/finished/error/stop).
- دمج منطق `same_ext` المكرر مرتين داخل `group_sliding_window` في CLI (استخراج دالة كما فعل GUI في `_split_by_extension`).

### تجربة المطور (DX)
- `pyproject.toml` مع `[project.scripts]` (تثبيت `pip install -e .` وأمر `file-finder` مباشر) بدل سكربتات حرة.
- إعداد ruff في `pyproject.toml` (قواعد F, E, B, UP) + pre-commit hook.
- CI: workflow واحد — lint + pytest + (لاحقاً) بناء PyInstaller بالـ matrix على المنصات الثلاث ورفع الـ artifacts في الـ Releases (مذكور أصلاً في خارطة الطريق).
- إضافة `CHANGELOG.md` — الإصدار 3.0 معلن بلا سجل تغييرات.

### تقليل الديون التقنية
بالترتيب: H-1 (توحيد المحرك) ← H-2 (اختبارات+CI) ← L-1 (تنظيف الميت) ← تفكيك الـ monolith ← ثم التفكير في PyQt6.

---

## 7. ميزات جديدة مقترحة (مرتبة قيمة/جهد)

1. **حارس الاحتفاظ بنسخة واحدة** + زر "تحديد الكل عدا الأحدث/الأكبر في كل مجموعة" — هذا نمط الاستخدام الفعلي لأدوات المكررات (اختيار الضحايا تلقائياً).
2. **`--exclude` و `--keep-newest` في CLI** لتمكين الأتمتة الكاملة.
3. **مقارنة مجلدين** (موجودة في خارطة الطريق — المحرك الحالي يدعمها بسهولة بعد التوحيد).
4. إحصائية "أكبر المجموعات توفيراً" مرتبة تنازلياً في أعلى النتائج.
5. i18n كامل عبر `QTranslator` (المرحلة 2 المعلنة) — البنية الحالية (نصوص حرفية في ~150 موضعاً) ستجعل التأجيل أغلى كل شهر.

---

## 8. خطة التطوير التدريجية (الأثر الأعلى ÷ التكلفة الأدنى أولاً)

| المرحلة | المحتوى | الجهد التقديري | لماذا بهذا الترتيب |
|---------|---------|----------------|---------------------|
| **0 — إصلاحات فورية** | بنود جدول القسم 5 (الأولويات 1-4) + `ruff --fix` | ساعات | أخطاء مؤكدة، إصلاحات سطرية، صفر مخاطرة |
| **1 — شبكة الأمان** | استخراج `core.py` موحد (H-1 الحل ج) + ~15 اختبار pytest للمحرك + GitHub Actions (ruff+pytest) | 1-2 يوم | كل ما بعده يصبح آمناً؛ يحل أكبر دينَين دفعة واحدة |
| **2 — سلامة البيانات** | حارس الاحتفاظ بنسخة، سجل النوايا قبل النقل، إيقاف فوري (M-1)، كاش ذرّي (M-3) | 1-2 يوم | الأداة تمس ملفات المستخدم — الأمان قبل الميزات |
| **3 — الأداء** | تجزئة متوازية + تحسين ملء الشجرة | 1 يوم | مكسب ملموس (2-4×) بعد أن صار المحرك مُختبراً |
| **4 — إعادة الهيكلة الكاملة** | تفكيك الحزمة (القسم 6-المعمارية) + checkboxes حقيقية + توحيد الخيوط + pyproject.toml | 3-5 أيام | الآن فقط، بحماية الاختبارات |
| **5 — التوزيع** | CI يبني PyInstaller للمنصات الثلاث + Releases + CHANGELOG | 1 يوم | يُبنى فوق CI القائم من المرحلة 1 |
| **6 — ميزات** | مقارنة مجلدين، اختيار الضحايا الذكي، i18n | حسب الرغبة | فوق أساس نظيف |

> **قاعدة ذهبية للمشروع:** لا ميزة جديدة قبل إنهاء المرحلتين 1 و 2 — كل ميزة تُضاف على المحرك المزدوج غير المُختبر تُضاعف كلفة إصلاحه لاحقاً.

---

## 9. أسئلة مفتوحة لمالك المشروع

1. **الرخصة**: هل النية مفتوح المصدر أم ملكية خاصة؟ (يحدد حل M-6).
2. **جمهور CLI**: هل يُستخدم فعلاً في أتمتة؟ (يحدد أولوية توثيق أكواد الخروج و `--exclude`).
3. **حجم الاستخدام الواقعي**: هل تُمسح مجلدات بمئات الآلاف من الملفات؟ (يحدد أولوية SQLite للكاش والترحيل الصفحي للنتائج).
4. **الترحيل إلى PyQt6**: هل من قيود تمنع رفع الحد الأدنى لبايثون إلى 3.9+؟ (README يعلن 3.7+ وهو منتهي الدعم أصلاً).

---

*انتهى التقرير — لم يُجرَ أي تعديل على الكود ضمن هذه المراجعة؛ كل الإصلاحات المقترحة بانتظار الموافقة.*
