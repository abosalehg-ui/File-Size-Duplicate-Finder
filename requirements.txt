# تبعيات تشغيل الواجهة الرسومية (GUI) فقط.
# المحرك والـ CLI (finder.cli) لا يحتاجان أي تبعية خارجية — راجع pyproject.toml.
# للتثبيت كحزمة بدلاً من ذلك:  pip install -e ".[gui]"
PyQt5>=5.15
send2trash>=1.8
