# سجل التغييرات — Changelog

## 4.1.1 — إصلاح تخطيط ولقطات محدَّثة

- **إصلاح تراكب العناصر عند فتح لوحة السجل**: كان الطول الأدنى للنافذة
  مفروضاً (`setMinimumSize(1040, 700)`) أصغر من حاجة التخطيط الحقيقية، فيقتطع
  Qt المحتوى فتُرسم أزرار التحديد فوق صفوف الشجرة وتتراكب حقول المعاينة مع
  أزرارها. الآن يُفرض العرض الأدنى فقط ويحسب Qt الطول من التخطيط، فيرفض
  التقليص بدل أن يقتطع. مع ذلك:
  - شجرة النتائج ولوحة المعاينة تتقلّصان بمرونة (`QSizePolicy.Ignored`).
  - تفاصيل المعاينة داخل منطقة تمرير وأزرارها مثبّتة أسفلها.
  - لوحة السجل تأخذ ارتفاعاً ابتدائياً معقولاً (190px) بدل حجمها المقترح
    الكبير الذي كان يقلّص الشجرة إلى سطر واحد.
  - اختبار انحدار يقيس عدم التراكب عند أضيق نافذة مسموحة مع فتح اللوحة.
- **لقطات README محدَّثة بأسماء جديدة** (`ui-light-results` / `ui-dark-results`
  / `ui-dry-run`): الأسماء القديمة كانت تُخزَّن مؤقتاً في GitHub فتُعرض لقطات
  التصميم القديم رغم تحديث الملفات.

## 4.1.0 — إعادة تصميم الواجهة

إعادة تصميم كاملة للواجهة بلا أي تغيير في محرك البحث أو منطق العمليات:
النتائج والضمانات كما هي، والمتغيّر هو كيف تُعرض وكيف يُتحكَّم بها.

### نظام تصميم بدل الألوان المتفرقة
- **`gui/theme.py`**: رموز ألوان ومقاسات (tokens) لكل وضع — الوضعان الفاتح
  والداكن يُبنيان من مصدر واحد. لا لون مكتوب داخل منطق الواجهة بعد اليوم:
  حُذفت كل استدعاءات `setStyleSheet` المتناثرة على الأزرار (كان لكل زر لونه
  الخاص: أزرق وبرتقالي وبنفسجي وأخضر وتركوازي في شاشة واحدة).
- **تراتُب بصري للأزرار**: نوع واحد أساسي (`primary`) لكل منطقة، والمدمّر
  (`danger`) للحذف وحده، والبقية محايدة أو شفافة — عبر خاصية `variant`
  في QSS بدل تنسيق يدوي لكل زر.
- **`QPalette` مع QSS**: ما يرسمه Qt نفسه (مؤشرات الاختيار، الأسهم، أشرطة
  التمرير) يتبع الثيم أيضاً، فلا تظهر عناصر فاتحة داخل الوضع الداكن.
- حُذف `gui/styles.py` (قالبا QSS مكتوبان يدوياً).

### أيقونات متسقة بدل الإيموجي
- **`gui/icons.py`**: طقم أيقونات SVG أحادي السماكة (36 أيقونة) يُلوَّن من
  الثيم ويُخزَّن مؤقتاً، مع حالة معطّلة باهتة. حُذفت الإيموجي من كل نصوص
  الأزرار والتبويبات والعناوين — كانت تختلف شكلاً وحجماً بين المنصات.
- كل أيقونة مربوطة بمعنى ثابت في كل الواجهة (السلة للحذف، الصندوق للعزل،
  السهم الراجع للإرجاع)، وتُعاد تلوينها كلها عند تبديل الثيم.

### ترتيب الواجهة
- **حُذف البانر المتدرّج** الذي كان يستهلك ربع الشاشة دون معلومة واحدة.
- **شريط قوائم وشريط أدوات**: كل إجراء له مكان واحد واختصار لوحة مفاتيح
  (ملف / التحديد / الإجراءات / عرض / مساعدة).
- **ثلاث مناطق تتبع تدفّق العمل**: بطاقة نطاق البحث ← النتائج ← شريط
  إجراءات سفلي. الإجراء الأساسي زر واحد يتحول إلى «إيقاف» أثناء العمل.
- **لوحة معاينة جانبية** بدل شريط أفقي أسفل النتائج: مساحة الشجرة تضاعفت،
  والمعاينة تعرض حقولاً مرتبة مع «فتح الموقع» و«نسخ المسار».
- **سجل النشاط لوحة سفلية** (Ctrl+L) بدل تبويب كامل — لم تعد رؤية السجل
  تعني إخفاء النتائج.
- **رقائق إحصاء** في صف الترويسة (مجموعات / ملفات / الحجم / ما يمكن تحريره)
  بدل سطر نصي طويل.

### قابلية الاستخدام
- **شريط الإجراءات يتفاعل مع التحديد**: يعرض «محدد: 9 ملفات في 4 مجموعات ·
  2.96 MB» ويبقى «عزل» و«سلة المحذوفات» معطّلين حتى يوجد تحديد فعلي — بدل
  السماح بالضغط ثم إظهار «الرجاء التحديد».
- **تحذير مبكر**: تنبيه أحمر يظهر في شريط الإجراءات لحظة تحديد كل ملفات
  مجموعة، لا في نافذة التأكيد وحدها.
- **عمود المجلد** في النتائج (نسبةً إلى جِذر الفحص) — كان تمييز النسخ
  المتطابقة الاسم يتطلب النقر على كل ملف.
- **ترتيب بالأعمدة** بقيمها الحقيقية (الحجم يُرتَّب رقمياً لا أبجدياً).
- **حالات فارغة موصوفة**: قبل البحث، ولا نتائج، ولا مطابقات للفلتر — كل
  واحدة بأيقونتها وتلميحها بدل شجرة بيضاء صامتة.
- **شرح وضع الكشف** سطراً واحداً تحت الخيارات، مع تنبيه إن كان حد التقارب
  غير مناسب للوضع المختار، ودليل استخدام كامل (F1).
- **قائمة سياق أغنى**: فتح الموقع، نسخ المسار، تحديد/إلغاء الملف أو المجموعة،
  توسيع/طيّ.
- **حفظ إعدادات أكثر**: الوضع المتداخل، وضع الكشف، حجم النافذة، توزيع
  المقسّم، وظهور لوحة المعاينة.
- **صياغة عربية صحيحة للأعداد** (`gui/textfmt.py`): «ملف واحد» و«ملفان»
  و«5 ملفات» و«15 ملفاً» بدل «1 ملفات»، وعزل اتجاهي للمسارات والأحجام فلا
  يظهر «MB 331.80» ولا ينقلب المسار.
- **تمييز المجموعات** بمربّع لون صغير وخلفية موحّدة بدل تلوين الصف كله
  بخمسة عشر لوناً.

### الاختبارات
- 23 اختباراً جديداً: `test_textfmt.py` (صياغة الأعداد وعزل الاتجاه، تعمل في
  الـ CI) و `test_gui_smoke.py` (بناء النافذة، الثيمان، حالة التحديد،
  الفلترة، الترتيب — تُتجاوَز حيث لا تتوفر PyQt5).

## 4.0.1 — خطة الإصلاح بعد المراجعة الشاملة

تنفيذ ملاحظات المراجعة المرتبة بالأولوية (لا تغييرات كاسرة للسلوك):

### السلامة والأمان
- **تحذير عدم تطابق المحتوى يشمل وضع Partial**: نافذة المعاينة كانت تحذّر في
  وضع الحجم فقط؛ الآن تحذّر أيضاً أن Partial قد يعطي تطابقاً كاذباً لملفات
  كبيرة تختلف في وسطها (`gui/main_window._content_warning`).
- **تحقق صريح بدل `assert` في كاش SQLite**: اسم عمود الـhash يُدرَج في نص
  SQL، فاستُبدل `assert kind in (...)` بـ `ValueError` حتى يبقى فعّالاً تحت
  `python -O` (`core/cache._check_kind`).

### الجودة الهندسية
- **وحدة تحديد نقية قابلة للاختبار** (`gui/selection.py`): فُصل منطق «التحديد
  الذكي» وحالة صندوق المجموعة الثلاثية عن عناصر Qt، مع 9 اختبارات جديدة.
- **إزالة تكرار التصدير**: الواجهة تستخدم الآن `reports.write_report` الموحّد
  (يضيف تصدير JSON من الواجهة) بدل تكرار اختيار الصيغة.
- **تقليص النافذة الرئيسية**: استُخرجت صفوف الأزرار إلى دوال مساعدة.
- تنظيف: توحيد `info` في `conftest`، حذف استيراد ميت، توضيح `requirements.txt`.

### الواجهة وإمكانية الوصول
- **اتساق الوضع الداكن**: نُقلت الألوان الثابتة (شريط الإحصائيات، ملخص
  وتحذيرات نافذة المعاينة) إلى `styles.py` لتتبع الثيم.
- **اختصارات وأسماء وصفية**: F5 بحث، Ctrl+O استعراض، Ctrl+F فلترة،
  Ctrl+S تصدير، مع `accessibleName` للعناصر الأساسية.

## 4.0.0 — 2026-07-19

إعادة هيكلة كاملة بناءً على المراجعة الهندسية الشاملة (`CODE_REVIEW.md`).

### المعمارية
- **حزمة `finder/` موحدة**: محرك واحد (`finder/core`) تستهلكه الواجهة الرسومية
  والـ CLI معاً — انتهى الازدواج الكامل الذي كان بين `file_size_duplicate_finder.py`
  و `file_finder_cli.py` (كان أكبر دين تقني في المشروع).
- طبقات واضحة: `core` (مسح/تجميع/تجزئة/كاش) ← `ops` (نقل/حذف/استرجاع/سجل)
  ← `gui` و `cli`. الملفان القديمان أصبحا نقطتَي دخول رفيعتين متوافقتين.
- خيط عامل موحد (`Worker`) بدل أربعة أصناف QThread متطابقة البنية.

### الأداء
- **تجزئة متوازية** (`ThreadPoolExecutor`) في وضعَي Partial و SHA-256 —
  تسريع 2-4× على أقراص SSD/NVMe.
- **كاش hash على SQLite** بدل JSON: كتابة ذرّية، بلا قراءة/كتابة كاملة في كل
  بحث، مصمم لمئات آلاف الملفات، مع ترحيل تلقائي للكاش القديم وتقليم
  للمدخلات البائتة.
- ملء شجرة النتائج بتحديثات معلّقة، وتوسيع تلقائي فقط حتى 100 مجموعة.
- تحديث النتائج محلياً بعد النقل/الحذف بدل إعادة البحث الكامل.

### سلامة البيانات
- **حارس الاحتفاظ بنسخة**: تحديد كل ملفات مجموعة يتطلب إقراراً صريحاً في
  نافذة المعاينة قبل تفعيل زر التأكيد.
- **تحديد ذكي**: «الكل عدا الأحدث» / «الكل عدا الأقدم» — تبقى نسخة دائماً.
- **سجل نوايا**: دفعة النقل تُكتب للقرص قبل بدء التنفيذ؛ الانقطاع يُوسم
  `interrupted` بدل الضياع.
- **استرجاع جزئي**: فشل إرجاع بعض الملفات لا يقفل العملية — يبقى المتبقي
  قابلاً لإعادة المحاولة.
- تحذير في المعاينة عند العمل بوضع "حجم متقارب" (التقارب لا يعني تطابق المحتوى).
- إيقاف فوري: علم الإلغاء يُفحص عند كل chunk قراءة — لا تجمّد للواجهة.

### إصلاحات
- `closeEvent` يوقف الآن كل الخيوط الحية بما فيها خيط سلة المحذوفات
  (كان يُنسى فيستمر الحذف بعد الإغلاق).
- التحديد الجماعي والعمليات تستثني الصفوف المخفية بالفلتر (WYSIWYG).
- حد التقارب في الواجهة يقبل 0 (تطابق حجم دقيق) — تطابقاً مع الـ CLI والتوثيق.
- checkboxes حقيقية (`Qt.ItemIsUserCheckable`) بدل رموز نصية — أفضل وصولاً
  وأبسط منطقاً.
- ألوان المجموعات لها طقم داكن مخصص مع لون نص صريح (كانت غير مقروءة
  في الوضع الداكن).
- "تاريخ الإنشاء" لا يُعرض إلا حيث يتوفر بدقة (Windows / أنظمة بـ st_birthtime).
- السحب والإفلات يحفظ `last_folder` كالاستعراض.
- إزالة كل الكود الميت (`file_paths`، dataclasses غير مستخدمة، 18 استيراداً،
  شرط QtMultimedia بلا معنى).

### CLI
- خيارات جديدة: `--exclude` (يقبل التكرار)، `--workers`، `--no-cache`, `--version`.
- `--quiet` يكتم كل المخرجات فعلاً.
- أكواد خروج موثقة: 0 = وُجدت مجموعات، 1 = لا نتائج، 2 = خطأ استخدام، 130 = إلغاء.

### البنية التحتية
- **اختبارات**: 47 اختبار pytest للمحرك والعمليات والسجل والـ CLI.
- **CI**: GitHub Actions — ruff + pytest على بايثون 3.9 و 3.12 لكل push/PR.
- **بناء آلي**: workflow يبني تنفيذيات PyInstaller للمنصات الثلاث عند إصدار tag.
- `pyproject.toml` مع أوامر `file-finder` و `file-finder-gui`.
- **رخصة MIT** (بقرار المالك — كان المستودع بلا ملف رخصة).
- رفع الحد الأدنى لبايثون إلى **3.9+**.
- `build.spec`: إزالة QtMultimedia غير الضروري، `upx=False` (يكسر Qt DLLs أحياناً).

## 3.0 — 2025

- كشف التكرار الحقيقي بالـ hashing (Partial + Full SHA-256).
- البحث المتداخل + خوارزمية النافذة المنزلقة O(n log n).
- ميزات الأمان: Dry-run، send2trash، نسخ احتياطية دوّارة.
- تحسينات الواجهة: سحب وإفلات، وضع داكن، فلتر مباشر، معاينة صور.
- واجهة سطر أوامر + PyInstaller spec.
